# PXD051304 annotation report

Palvimo lab (University of Eastern Finland, Kuopio). GR and SUMO2/3 RIME in NALM-6 cells ± dexamethasone ± ML792, on Bruker timsTOF Pro. Not in the community corpus.

## Templates

- `ms-proteomics` v1.1.0
- `human` v1.1.0
- `cell-lines` v1.1.0 (NALM-6 / CVCL_0092; OLS label `NALM-6 cell`)

RIME chromatin fixation is not the `crosslinking` template.

## Rows and files

12 data rows, one per deposited `.d.zip`, biological replicate `pooled` (zip names `rep1-3`; FragPipe columns `*_rep1_*` / `rep2` / `rep3`). Inner `.d` names were not range-listed.

## Factors

- `factor value[antibody]`: GR / SUMO2/3 / rabbit IgG / mouse IgG
- `factor value[glucocorticoid]`: dexamethasone / ethanol / not available (SUMO and mouse-IgG zips have no Dex/EtOH token)
- `factor value[SUMO inhibitor]`: ML792 / DMSO

## Conservative choices

- Disease is the NALM-6 Cellosaurus disease on every row. Filename `CTRL` is DMSO, not a healthy donor (reconcile `disease_on_control_runs` is a false positive).
- Ancestry Caucasian from Cellosaurus.
- Same FragPipe modification set as PXD039179.

## Validation

`structure` OK; `check` CLEAN. Adversarial review-gate was not run (Oulu protocol).
