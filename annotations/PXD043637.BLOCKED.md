# PXD043637 — BLOCKED

- **Evidence:** PRIDE deposits three label-free RAW files named `sample01`, `sample02`, and `sample05`.  Its `221101_GMLO-Andra_IPMS_protein_results.xlsx` workbook maps `sample01` and `sample02` to `myc-FANCD2-K561R_1` and `_2`, but maps its only further sample identifier as `sample03` (`myc-FANCD2-AA`).
- **Why blocked:** No inspected source resolves the archive's `sample05` to the workbook's `sample03` or to a biological condition.  Emitting a three-run SDRF would fabricate that missing association.
- **Unblocking requirement:** A submitted run sheet or author confirmation identifying `sample05` and reconciling it with the reported `sample03` condition.
