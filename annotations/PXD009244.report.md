# PXD009244 annotation report

## Scope

All 54 deposited RAW archives are represented as label-free oral-tongue tissue assays.

## Evidence and decisions

- The deposited `Metadata.xlsx` supplies the exact mapping between each archived RAW base name, paired cancer/control tissue sample, sample category, and clinical stage. All are carried into the SDRF without inference.
- Every sample is represented by three consecutive archived RAW files; the technical replicate is derived from that deposited file sequence. Source sample IDs remain as submitted.
- The repository stores each acquisition as `.raw.zip`; the data-file field retains this addressable archive name rather than assuming an extracted local file.
- PRIDE specifies paired healthy/oral-tongue squamous-cell-carcinoma tissues, Trypsin digestion, label-free Synapt G2-S acquisition, and no explicit search modifications.

## Validation

Validated with `parse_sdrf validate-sdrf --template ms-proteomics --skip-ontology`.
