# PXD007762 annotation report

## Scope

All 76 deposited RAW files are represented as label-free, time-resolved assays.

## Evidence and decisions

- The `20160202` files are the HCT116 series: two replicates every 6 h after DMSO vehicle, methotrexate, or paclitaxel treatment, with two baseline (`T0`) files. The deposited protocol assigns this series to an Orbitrap Fusion.
- The `20161115_TS_OSW` files are the A375 series: three replicates every 12 h for DMSO vehicle (`D`) or untreated (`C`) cultures, with three baseline files. The deposited protocol assigns this series to a Q Exactive Plus.
- Treatment, duration, and replicate derive directly from the file labels and processing protocol. Digestion and modification fields follow the deposited method/search settings.

## Validation

Validated with `parse_sdrf validate-sdrf --template ms-proteomics --skip-ontology`.
