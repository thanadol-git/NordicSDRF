# PXD071373 annotation report

Palvimo lab (University of Eastern Finland, Kuopio). CEBPA and SUMO2/3 RIME in BLaER1 cells during E2-induced B-cell-to-macrophage transdifferentiation ± ML792. Not in the community corpus.

## Templates

- `ms-proteomics` v1.1.0
- `human` v1.1.0

`cell-lines` was not used: BLaER1 has no Cellosaurus hit. `characteristics[cell line]=BLaER1` is an extra column.

RIME chromatin fixation is not the `crosslinking` template.

## Rows and files

17 data rows, one per deposited `.d.zip`, biological replicate `pooled` (`rep1-3` in the zip name). Inner `.d` names were not range-listed. CEBPA FragPipe columns are numbered `26616_AT_*` rather than condition names; zip filenames are the sample map.

## Factors

- `factor value[antibody]`: CEBPA / SUMO2/3 / rabbit IgG / mouse IgG
- `factor value[time]`: 0 / 6 / 24 hour after E2
- `factor value[SUMO inhibitor]`: DMSO / ML792 / not available (SUMO23 and mouse-IgG zips have no DMSO/SUMOi token)

## Conservative choices

- Disease `acute leukemia` from the PRIDE record; BLaER1 is a leukemia transdifferentiation model. Filename `DMSO` is the vehicle arm, not a healthy control.
- Age and sex `not available`.
- Same FragPipe modification set as the other Kuopio RIME deposits.

## Validation

`structure` OK; `check` CLEAN. Adversarial review-gate was not run (Oulu protocol).
