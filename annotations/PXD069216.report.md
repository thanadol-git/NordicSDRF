# PXD069216 annotation report

## Scope

All 80 RAW files are represented. Their names encode PANC-1 sample, deposited Scr or siRNA experimental-group code, biological replicate 1–4, and gel fraction 1–10. The timestamped first siRNA run is retained exactly as deposited. The group codes are not expanded beyond the submitted labels.

## Validation

Validated with parse_sdrf validate-sdrf --template ms-proteomics --skip-ontology.

